import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'MemoryVault - Digital Time Capsules for Your Precious Moments',
  description: 'Create interactive digital time capsules with photos, videos, voice notes and text. Perfect for preserving memories and creating surprise reveals for special occasions.',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  )
}